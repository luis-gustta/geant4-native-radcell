diff --git a/RADCellSimulation/CMakeLists.txt b/RADCellSimulation/CMakeLists.txt
index 0202bc5..3d402c9 100755
--- a/RADCellSimulation/CMakeLists.txt
+++ b/RADCellSimulation/CMakeLists.txt
@@ -1,6 +1,6 @@
 #----------------------------------------------------------------------------
 # Setup the project
-cmake_minimum_required(VERSION 2.6 FATAL_ERROR)
+cmake_minimum_required(VERSION 3.5)
 project(RADCellSimulation)
 
 #----------------------------------------------------------------------------
@@ -42,7 +42,8 @@ set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} ${OpenMP_CXX_FLAGS}")
 set(CMAKE_EXE_LINKER_FLAGS "${CMAKE_EXE_LINKER_FLAGS} ${OpenMP_EXE_LINKER_FLAGS}")
 endif()
 
-add_library(radcelllib SHARED ${sources}) # creating a libarary for radcell ,changed here 
+add_library(radcelllib SHARED ${sources})
+target_link_libraries(radcelllib ${Geant4_LIBRARIES} ${PYTHON_LIBRARIES}) # creating a libarary for radcell ,changed here 
 find_package(PythonLibs REQUIRED) #find python libs for calling python function in C++
 include_directories(${PYTHON_INCLUDE_DIRS})
 
diff --git a/RADCellSimulation/include/Analysis.hh b/RADCellSimulation/include/Analysis.hh
index 12f0f39..596e8ed 100755
--- a/RADCellSimulation/include/Analysis.hh
+++ b/RADCellSimulation/include/Analysis.hh
@@ -12,6 +12,6 @@
 #ifndef Analysis_h
 #define Analysis_h 1
 
-#include "g4root.hh"
+#include "G4AnalysisManager.hh"
 
 #endif
diff --git a/RADCellSimulation/include/RADCellSimulation.hh b/RADCellSimulation/include/RADCellSimulation.hh
index 6c6238e..a2f1ba3 100755
--- a/RADCellSimulation/include/RADCellSimulation.hh
+++ b/RADCellSimulation/include/RADCellSimulation.hh
@@ -4,15 +4,9 @@
 // December 10,2015
 // This is a class for running the radiation transportation in cells
 
-#ifdef G4MULTITHREADED
-#include "G4MTRunManager.hh"
-#else
 #include "G4RunManager.hh"
-#endif
 #include "DetectorConstruction.hh"
 #include "G4UImanager.hh"
-#include "G4VisExecutive.hh"
-#include "G4UIExecutive.hh"
 #include "G4Types.hh"
 #include <string>
 
@@ -39,14 +33,10 @@ class RADCellSimulation
       private:
       
       DetectorConstruction* detector;
-      G4VisManager* visManager;
-        #ifdef G4MULTITHREADED
-        G4MTRunManager* runManager;
-        #else
+      void* visManager;
         G4RunManager* runManager;
-        #endif
       G4UImanager* UImanager;
-      G4UIExecutive* ui; 
+      void* ui; 
 
       
       
diff --git a/RADCellSimulation/python_wrapper/CMakeLists.txt b/RADCellSimulation/python_wrapper/CMakeLists.txt
index 792a05b..9dbfc5c 100755
--- a/RADCellSimulation/python_wrapper/CMakeLists.txt
+++ b/RADCellSimulation/python_wrapper/CMakeLists.txt
@@ -1,5 +1,5 @@
 # I don't really know why these two lines are needed!
-CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
+cmake_minimum_required(VERSION 3.5)
 PROJECT(PYINTERFACE)
 
 #include(${CMAKE_CURRENT_SOURCE_DIR}/FindSWIG.cmake) # this is slightly modified FindSWIG.cmake file -  had to tune it to work with OSX
@@ -25,48 +25,7 @@ ENDIF(SWIG_FOUND)
 #       OUTPUT_VARIABLE NUMPY_INCLUDE_DIR)
 
 
-SET(LIBS
-   radcelllib   
-   G4vis_management
-   G4digits_hits 
-   G4geometry 
-   G4GMocren 
-   G4materials
-   G4particles 
-   G4RayTracer
-   G4track 
-   G4visXXX
-   G4error_propagation
-   G4geomUSolids
-   G4graphics_reps  
-   G4modeling   
-   G4persistency   
-   G4readout    
-   G4Tree            
-   G4VRML
-   G4analysis  
-   G4event              
-   G4gl2ps        
-   G4intercoms      
-   G4OpenGL     
-   G4physicslists  
-   G4run        
-   G4visHepRep       
-   G4zlib
-   G4clhep     
-   G4FR                 
-   G4global       
-   G4interfaces     
-   G4parmodels  
-   G4processes     
-   G4tracking   
-   G4vis_management
-   pthread
-   ${PYTHON_LIBRARIES}
-
-)
-
-
+SET(LIBS radcelllib ${Geant4_LIBRARIES} ${PYTHON_LIBRARIES} pthread)
 INCLUDE_DIRECTORIES(${PYTHON_INCLUDE_PATH} ${CMAKE_CURRENT_SOURCE_DIR} ${CMAKE_SOURCE_DIR}/include)
  
 SET_SOURCE_FILES_PROPERTIES(radcell.i PROPERTIES CPLUSPLUS ON)
diff --git a/RADCellSimulation/src/DataProcessUsingPython.cc b/RADCellSimulation/src/DataProcessUsingPython.cc
index 0e71245..28d7b7e 100755
--- a/RADCellSimulation/src/DataProcessUsingPython.cc
+++ b/RADCellSimulation/src/DataProcessUsingPython.cc
@@ -17,7 +17,7 @@ void DataProcessUsingPython::PlotCellSurvivalCurve(string fileName, string color
     vec_str.push_back("plotCellSF");
     vec_str.push_back("plotSFCurve");
 
-    vec_str.push_back(fileName.c_str()); // the input argument for PyString_FromString is const char*, so use this line
+    vec_str.push_back(fileName.c_str()); // the input argument for PyUnicode_FromString is const char*, so use this line
     vec_str.push_back(color.c_str());
 //     vec_str.push_back("'cellState.csv'");
 //     vec_str.push_back("'b'");
@@ -42,7 +42,7 @@ void DataProcessUsingPython::PlotCellSurvivalCurve(string fileName, string color
     }
 
     Py_Initialize();
-    pName = PyString_FromString(argv1[1]);
+    pName = PyUnicode_FromString(argv1[1]);
     /* Error checking of pName left out */
 
     pModule = PyImport_Import(pName);
@@ -55,8 +55,8 @@ void DataProcessUsingPython::PlotCellSurvivalCurve(string fileName, string color
         if (pFunc && PyCallable_Check(pFunc)) {
             pArgs = PyTuple_New(argc - 3);
             for (i = 0; i < argc - 3; ++i) {
-//                 pValue = PyString_FromString(atoi(argv1[i + 3]));// if need number argument, use this line
-                pValue = PyString_FromString((argv1[i + 3])); // if need string argument, use this line
+//                 pValue = PyUnicode_FromString(atoi(argv1[i + 3]));// if need number argument, use this line
+                pValue = PyUnicode_FromString((argv1[i + 3])); // if need string argument, use this line
                 if (!pValue) {
                     Py_DECREF(pArgs);
                     Py_DECREF(pModule);
@@ -68,7 +68,7 @@ void DataProcessUsingPython::PlotCellSurvivalCurve(string fileName, string color
             pValue = PyObject_CallObject(pFunc, pArgs);
             Py_DECREF(pArgs);
             if (pValue != NULL) {
-                printf("Result of call: %ld\n", PyInt_AsLong(pValue));
+                printf("Result of call: %ld\n", PyLong_AsLong(pValue));
                 Py_DECREF(pValue);
             }
             else {
diff --git a/RADCellSimulation/src/PhysicsList.cc b/RADCellSimulation/src/PhysicsList.cc
index 26f68a5..a3f2dc5 100755
--- a/RADCellSimulation/src/PhysicsList.cc
+++ b/RADCellSimulation/src/PhysicsList.cc
@@ -139,12 +139,13 @@ void PhysicsList::ConstructProcess()
 void PhysicsList::ConstructEM()
 {
 
-  theParticleIterator->reset();
+  auto particleIterator = GetParticleIterator();
+  particleIterator->reset();
 
-  while( (*theParticleIterator)() )
+  while( (*particleIterator)() )
   {
 
-    G4ParticleDefinition* particle = theParticleIterator->value();
+    G4ParticleDefinition* particle = particleIterator->value();
     G4ProcessManager* pmanager = particle->GetProcessManager();
     G4String particleName = particle->GetParticleName();
 
diff --git a/RADCellSimulation/src/RADCellSimulation.cc b/RADCellSimulation/src/RADCellSimulation.cc
index 6ecbb71..12edcee 100755
--- a/RADCellSimulation/src/RADCellSimulation.cc
+++ b/RADCellSimulation/src/RADCellSimulation.cc
@@ -5,13 +5,8 @@
 
 #include "RADCellSimulation.hh"
 #include "G4UImanager.hh"
-#include "G4UIExecutive.hh"
-#include "G4UIterminal.hh"
-#include "G4UItcsh.hh"
-#include "G4VisExecutive.hh"
 #include "ActionInitialization.hh"
 #include "PhysicsList.hh"
-#include "G4UIQt.hh"
 #include "G4SystemOfUnits.hh"
 #include "G4String.hh"
 #include "G4GeometryManager.hh"
@@ -30,15 +25,18 @@ using namespace std;
 RADCellSimulation::RADCellSimulation()
 {
     detector = new DetectorConstruction();
+    runManager = nullptr;
+    visManager = nullptr;
+    ui = nullptr;
+    UImanager = nullptr;
 }
 
 
 RADCellSimulation::~RADCellSimulation()
 {
-    delete visManager;
-    delete runManager;
-    delete ui;
-
+    // Intentionally empty.
+    // Geant4 owns global/singleton state and Python/SWIG shutdown order is unsafe here.
+    // Do not delete runManager, detector, visManager or ui from the Python wrapper destructor.
 }
 
 
@@ -101,9 +99,8 @@ void RADCellSimulation::RADCellSimulationInitialize(int argc, char** argv)
 
 
   #ifdef G4MULTITHREADED
-   // G4MTRunManager* runManager = new G4MTRunManager;
-    runManager= new G4MTRunManager;
-    runManager->SetNumberOfThreads(4);// set up the number of number of threads for multithread simulation
+   // G4MTRunManager* runManager = new G4RunManager;
+    runManager= new G4RunManager;// set up the number of number of threads for multithread simulation
   #else
    // G4RunManager* runManager = new G4RunManager;
     runManager= new G4RunManager;
@@ -124,11 +121,9 @@ void RADCellSimulation::RADCellSimulationInitialize(int argc, char** argv)
   
     runManager->Initialize();
  
-  // Initialize visualization 
-  visManager = new G4VisExecutive;
-
-  visManager->Initialize();  // comment this if you want to run radcell in CC3D
-  ui = new G4UIExecutive(argc, argv); // 
+  // Initialize visualization
+    visManager = nullptr;  // comment this if you want to run radcell in CC3D
+    ui = nullptr; // 
   UImanager = G4UImanager::GetUIpointer();  // Get the pointer to the User Interface manager
   UImanager->ApplyCommand("/control/execute vis.mac");
 
@@ -174,15 +169,11 @@ void RADCellSimulation::EnergyDistributionCalculation(string runArgument,string
     ArgumentInterpreter::SetPostProcessMarker(postProMarker);
     
     
-    if (argument_vector[0]=="gui")// running in gui interactive mode 
-    {
-          // Define UI terminal for interactive mode  
-        //G4UIExecutive* ui = new G4UIExecutive(argc, argv);
-        
-        UImanager->ApplyCommand(runCommand);
-        ui->SessionStart(); 
-       // delete ui;
-    }
+    if (argument_vector[0]=="gui")
+  {
+    G4cerr << "RADCELL was compiled in headless mode; GUI mode is disabled. Use runArgument=\"out\"." << G4endl;
+    UImanager->ApplyCommand(runCommand);
+  }
     if (argument_vector[0]=="out")// running in output file mode
     { 
         UImanager->ApplyCommand(runCommand);
diff --git a/RADCellSimulation/src/RunAction.cc b/RADCellSimulation/src/RunAction.cc
index f2bb00d..9492e3c 100755
--- a/RADCellSimulation/src/RunAction.cc
+++ b/RADCellSimulation/src/RunAction.cc
@@ -349,7 +349,7 @@ void RunAction::EndWorker(const G4Run* run)
                     ///////////////
                     // Complete cleanup
                     //
-                    delete G4AnalysisManager::Instance();
+                    G4AnalysisManager::Instance()->Reset();
             }
             
             G4RunDoseAnalysis runDoseAnalysis;
diff --git a/VascularTumor/Simulation/RADCellSimulation.py b/VascularTumor/Simulation/RADCellSimulation.py
index 1c0a20f..d5884aa 100644
--- a/VascularTumor/Simulation/RADCellSimulation.py
+++ b/VascularTumor/Simulation/RADCellSimulation.py
@@ -5,11 +5,11 @@ import itertools
 
 from collections import OrderedDict
 
-print 'Start calling subprocess RADCellSimulation here'
-print 'The directory now is: ', os.getcwd()
+print('Start calling subprocess RADCellSimulation here')
+print('The directory now is: ', os.getcwd())
 cellInformationPath = os.getcwd() +"/cellInformation.csv"
-print 'the cellInformationPath now is ', cellInformationPath
-print 'check files is: ',os.path.isfile(cellInformationPath)
+print('the cellInformationPath now is ', cellInformationPath)
+print('check files is: ',os.path.isfile(cellInformationPath))
 
 try:
     ifile = open(cellInformationPath,"r")
@@ -34,7 +34,7 @@ except IOError:
 sim=radcell.RADCellSimulation()
 
 sim.SetCellWorld(dX,dY,dZ) # change unit here in mm
-print 'the dimension of tissue is ',dX, dY, dZ
+print('the dimension of tissue is ',dX, dY, dZ)
 
 # cell color and nucleus color could be as :
 # Gray, Grey, Black, Green, Red, Blue, Magenta, Yellow
@@ -53,7 +53,7 @@ sim.CreateCell(cellType_list[3],"Cytoplasma Nucleus","Sphere","Yellow Green")
 sim.SetCellSimulationParameter(1,"Nucleus")
 
 
-for key, value in cellInfoDict.items():
+for key, value in list(cellInfoDict.items()):
     sim.CellConstruction(int(key),cellType_list[value[3]-1],value[0],value[1],value[2],10,5)
 
 
@@ -71,12 +71,12 @@ for key, value in cellInfoDict.items():
 #         sim.CellConstruction(int(key),"Epithelia2",cell_x,cell_y,cell_z,2,1)
     
     
-print 'Before starting calling radiation energy deposition caclculation function \n'
+print('Before starting calling radiation energy deposition caclculation function \n')
 
 
-sim.RADCellSimulationInitializePyWrapper(len(sys.argv), sys.argv)
+sim.RADCellSimulationInitializePyWrapper(1, [sys.argv[0]])
 
-print 'the argument is ', sys.argv
+print('the argument is ', sys.argv)
 
 runMode=sys.argv[1]
 radiationSource = sys.argv[2]+".in"
diff --git a/VascularTumor/Simulation/RadiationTransportModule.py b/VascularTumor/Simulation/RadiationTransportModule.py
index da536bf..6026eac 100644
--- a/VascularTumor/Simulation/RadiationTransportModule.py
+++ b/VascularTumor/Simulation/RadiationTransportModule.py
@@ -1,6 +1,6 @@
 
-from PySteppables import *
-import CompuCell
+from cc3d.core.PySteppables import SteppableBasePy
+from cc3d.cpp import CompuCell
 import sys
 import os
 from scipy import interpolate
@@ -32,7 +32,7 @@ class WriteCellInformation():
         writer = csv.writer(ofile)
         writer.writerow([dX,dY,dZ])
         
-        for key, value in self.cellDict.items():
+        for key, value in list(self.cellDict.items()):
             cell_x=value[0]*self.DCF/TDF-dX/2.0  # this is changing coordinate system between CC3D and Geant4
             cell_y=value[1]*self.DCF/TDF-dY/2.0
             cell_z=value[2]*self.DCF/TDF-dZ/2.0
@@ -59,7 +59,7 @@ class RadiationTransport(SteppableBasePy):
         writeCellInfo = WriteCellInformation(dim,cellInfoDict,self.DCF) 
         cellInformationPath = os.path.dirname(__file__) +"/cellInformation.csv"
         writeCellInfo.Write(cellInformationPath)
-        child = subprocess.Popen(["python","RADCellSimulation.py",runMode,radiationSource],cwd=os.path.dirname(__file__))#check the geometry and radiation source
+        child = subprocess.Popen([sys.executable, "-u", "RADCellSimulation.py", runMode, radiationSource], cwd=os.path.dirname(__file__)) # check the geometry and radiation source
         child.wait()
 
 class RadiationSource(SteppableBasePy):
@@ -165,7 +165,7 @@ class CellSimulationResult():
             self.readResult.ReadDNADamageTallyOutPut(os.path.dirname(__file__)+"/"+str(self.fileName)+"_DNADamage.csv") # read normalized cell DSB file
             singleCellDoseFileName = self.singleCellFileName+"_dose.csv"
             singleCellDSBFileName = self.singleCellFileName+"_DNADamage.csv"
-            print 'single cell file name is ', singleCellDoseFileName, singleCellDSBFileName 
+            print('single cell file name is ', singleCellDoseFileName, singleCellDSBFileName) 
             self.readResult.ReadSingleCellDoseAsReference(os.path.dirname(__file__)+"/"+singleCellDoseFileName)
             self.readResult.ReadSingleCellDNADamageAsReference(os.path.dirname(__file__)+"/"+singleCellDSBFileName)
             self.readResult.GetAbsoluteResultsByCOIMethod(prescribed_dose, "indirect","mean")
diff --git a/VascularTumor/Simulation/VascularTumor.py b/VascularTumor/Simulation/VascularTumor.py
index 007feb6..6d0cf6e 100644
--- a/VascularTumor/Simulation/VascularTumor.py
+++ b/VascularTumor/Simulation/VascularTumor.py
@@ -1,30 +1,14 @@
+import os
 import sys
-from os import environ
-import string
-sys.path.append(environ["PYTHON_MODULE_PATH"])
 
+sys.path.append(os.path.dirname(__file__))
 
-
-import CompuCellSetup
-sim,simthread = CompuCellSetup.getCoreSimulationObjects()
-CompuCellSetup.initializeSimulationObjects(sim,simthread)
-import CompuCell
-
-#Create extra player fields here or add attributes
-
-
-from PySteppables import SteppableRegistry
-steppableRegistry=SteppableRegistry()
-
+from cc3d import CompuCellSetup
 
 from VascularTumorSteppables import MitosisSteppable
-mitosisSteppable=MitosisSteppable(sim,1)
-steppableRegistry.registerSteppable(mitosisSteppable)
-
 from VascularTumorSteppables import VolumeParamSteppable
-                                         #sim,frequency,areaThresh,nutrientThresh,necroticThresh
-volumeParamSteppable=VolumeParamSteppable(sim,1)
-steppableRegistry.registerSteppable(volumeParamSteppable)
 
-CompuCellSetup.mainLoop(sim,simthread,steppableRegistry)
+CompuCellSetup.register_steppable(steppable=MitosisSteppable(frequency=1))
+CompuCellSetup.register_steppable(steppable=VolumeParamSteppable(frequency=1))
 
+CompuCellSetup.run()
diff --git a/VascularTumor/Simulation/VascularTumorSteppables.py b/VascularTumor/Simulation/VascularTumorSteppables.py
index 78be2d9..8e97daf 100644
--- a/VascularTumor/Simulation/VascularTumorSteppables.py
+++ b/VascularTumor/Simulation/VascularTumorSteppables.py
@@ -1,6 +1,6 @@
-from PySteppables import *
-from PySteppablesExamples import MitosisSteppableBase
-import CompuCell
+from cc3d.core.PySteppables import *
+from cc3d.core.PySteppables import MitosisSteppableBase
+from cc3d.cpp import CompuCell
 import sys
 from random import uniform
 import math
@@ -11,11 +11,11 @@ import scipy.stats as stats
 import os
 
 class VolumeParamSteppable(SteppableBasePy):
-    def __init__(self,_simulator,_frequency=1,):
-        SteppableBasePy.__init__(self,_simulator,_frequency)
+    def __init__(self, frequency=1):
+        SteppableBasePy.__init__(self, frequency)
         self.fieldNameVEGF2 = 'VEGF2'
         self.fieldNameGlucose = 'Glucose'
-        self.numberOfTumorCellList = []  
+        self.numberOfTumorCellList = []
         self.numberOfVascularCellList = []
         self.numberOfNeovascularCellList = []
         self.numberOfNecroticCellList = []
@@ -39,25 +39,25 @@ class VolumeParamSteppable(SteppableBasePy):
             else:
                 cell.targetVolume=32.0
                 cell.lambdaVolume=20.0
-                
+
         DCF = 10
         dX = self.dim.x*DCF/float(1000) #change unit into mm
         dY = self.dim.y*DCF/float(1000)
         dZ = self.dim.z*DCF/float(1000)
-       
+
 
         radSource = RTM.RadiationSource('testInputSource')
 #         radSource.PlaneSource_MRT(0,0,dZ/2.0,dX/2.0,dY/20.0,'e-',1000,0)
         radSource.PlaneSource(0,0,dZ/2.0,dX/2.0,dY/20.0,'e-',1000,1000)
         radTransport = RTM.RadiationTransport(self.dim,self.cellList,DCF)# importing tissue and cell geometry from CC3D
-#         radTransport.Run("gui",radSource.GetSourceID()) # check geometry and radiation source 
-        
+#         radTransport.Run("gui",radSource.GetSourceID()) # check geometry and radiation source
+
 
 
     def step(self,mcs):
         fieldVEGF2=CompuCell.getConcentrationField(self.simulator,self.fieldNameVEGF2)
         fieldGlucose=CompuCell.getConcentrationField(self.simulator,self.fieldNameGlucose)
-        
+
 #         if mcs == 10:
 #         if mcs == 5000:
 #         if mcs == 7000:
@@ -71,21 +71,21 @@ class VolumeParamSteppable(SteppableBasePy):
             dX = self.dim.x*DCF/float(1000) #change unit into mm
             dY = self.dim.y*DCF/float(1000)
             dZ = self.dim.z*DCF/float(1000)
-           
+
 
 #             radSource = RTM.RadiationSource('testInputSource')
 #             radSource.PlaneSource(0,0,dZ/4.0,dX/100.0,dY/100.0,'e-',1000,0)
 
 #             radTransport = RTM.RadiationTransport(self.dim,self.cellList,DCF)# importing tissue and cell geometry from CC3D
-#             radTransport.Run("gui",radSource.GetSourceID()) # check geometry and radiation source 
-            
+#             radTransport.Run("gui",radSource.GetSourceID()) # check geometry and radiation source
+
             radSource = RTM.RadiationSource('testInputSource')
             radSource.PlaneSource_MRT(0,0,dZ/2.0,dX/2.0,dY/20.0,'e-',1000,1000)
 #             radSource.PlaneSource(0,0,dZ/2.0,dX/2.0,dY/20.0,'e-',1000,500)
             radTransport = RTM.RadiationTransport(self.dim,self.cellList,DCF)# importing tissue and cell geometry from CC3D
-            runMode = 'gui'+' ' + self.simulationID
-            print 'the runMode is: ', runMode
-#             radTransport.Run("gui",radSource.GetSourceID()) # check geometry and radiation source 
+            runMode = 'out'+' ' + self.simulationID
+            print('the runMode is: ', runMode)
+#             radTransport.Run("gui",radSource.GetSourceID()) # check geometry and radiation source
 #             radSource.PlaneSource(0,0,dZ/2.0,dX/2.0,dY/20.0,'e-',1000,5000)
             radTransport.Run(runMode,radSource.GetSourceID()) # check geometry and radiation source
             simResult = RTM.CellSimulationResult(self.simulationID)
@@ -98,25 +98,25 @@ class VolumeParamSteppable(SteppableBasePy):
                 E1 = 0 # mean state energy of S1 state
                 E3= 48.47 # mean state energy of S2 state
                 sigma = 6.96 # sigma of state energy distribution
-                
+
                 cellDose, cellDSB, normalized_dose,normalized_DSB= simResult.GetCellDose(cell.id)
 #                 print 'The dose for cell: ',cell.id, ' is: ',cellDose
 #                 print 'The DSB for cell: ', cell.id, 'is: ' , cellDSB
 #                 print 'The normalized dose for cell: ', cell.id, 'is: ', normalized_dose
 #                 print 'The normalized DSB for cell: ', cell.id, 'is: ', normalized_DSB
-                
+
                 delta_E = alpha*cellDSB # here we just consider the direct effect
                 x = -abs(E1+delta_E-E3)/2/sigma
-                print 'delta energy is ',x
+                print('delta energy is ',x)
                 p_total = 2*stats.norm.cdf(x,0,1)
                 if E1+delta_E-E3>=0:
                     p_total = 1# boundary condition
-                print 'the total jumping probability is: ',p_total
-                jumpingProbabilityPerMCS = p_total 
+                print('the total jumping probability is: ',p_total)
+                jumpingProbabilityPerMCS = p_total
                 if cellDose!=0:
                     if random.random()<jumpingProbabilityPerMCS:
                         cell.type = self.NECROTIC # if so, cell dies due to radiation killing
-        
+
         numberOfTumorCells = 0
         numberOfVascularCells = 0
         numberOfNeovascularCells = 0
@@ -133,39 +133,48 @@ class VolumeParamSteppable(SteppableBasePy):
                 # pt.x=int(round(cell.xCM/max(float(cell.volume),0.001)))
                 # pt.y=int(round(cell.yCM/max(float(cell.volume),0.001)))
                 # pt.z=int(round(cell.zCM/max(float(cell.volume),0.001)))
-                
+
                 # VEGFConcentration=fieldVEGF2.get(pt)
-                
+
                 VEGFConcentration=fieldVEGF2[int(round(cell.xCOM)),int(round(cell.yCOM)),int(round(cell.zCOM))]
-                
+
                 # cellNeighborList=CellNeighborListAuto(self.nTrackerPlugin,cell)
-                cellNeighborList=self.getCellNeighbors(cell)
+                cellNeighborList = self.getCellNeighborDataList(cell)
                 for neighborSurfaceData in cellNeighborList:
-                    #Check to ensure cell neighbor is not medium
-                    if neighborSurfaceData.neighborAddress:
-                        if neighborSurfaceData.neighborAddress.type == self.VASCULAR or neighborSurfaceData.neighborAddress.type == self.NEOVASCULAR:                            
-                            #sum up common surface area of cell with its neighbors
-                            totalArea+=neighborSurfaceData.commonSurfaceArea 
-                            #print "  commonSurfaceArea:",neighborSurfaceData.commonSurfaceArea
-                #print totalArea        
+                    # CC3D 4.x returns neighbor data as a tuple:
+                    # (neighbor_cell, common_surface_area).
+                    # Older CC3D returned an object with neighborAddress/commonSurfaceArea.
+                    if isinstance(neighborSurfaceData, tuple):
+                        neighbor_cell = neighborSurfaceData[0]
+                        common_surface_area = neighborSurfaceData[1]
+                    else:
+                        neighbor_cell = neighborSurfaceData.neighborAddress
+                        common_surface_area = neighborSurfaceData.commonSurfaceArea
+
+                    # Check to ensure cell neighbor is not medium
+                    if neighbor_cell:
+                        if neighbor_cell.type == self.VASCULAR or neighbor_cell.type == self.NEOVASCULAR:
+                            # sum up common surface area of cell with its neighbors
+                            totalArea += common_surface_area
+                #print totalArea
                 if totalArea < 45:
                     #Growth rate equation
-                    
+
                     cell.targetVolume+=2.0*VEGFConcentration/(0.01 + VEGFConcentration)
-                    print "totalArea", totalArea,"cell growth rat& voxel/MCS \\e: ", 2.0*VEGFConcentration/(0.01 + VEGFConcentration),"cell Volume: ", cell.volume
-         
+                    print("totalArea", totalArea,"cell growth rat& voxel/MCS \\e: ", 2.0*VEGFConcentration/(0.01 + VEGFConcentration),"cell Volume: ", cell.volume)
+
             #Proliferating Cells
             if cell.type == self.PROLIFERATING:
-                
+
                 numberOfTumorCells = numberOfTumorCells + 1 # tallying the tumor cells
                 # pt=CompuCell.Point3D()
                 # pt.x=int(round(cell.xCM/max(float(cell.volume),0.001)))
                 # pt.y=int(round(cell.yCM/max(float(cell.volume),0.001)))
                 # pt.z=int(round(cell.zCM/max(float(cell.volume),0.001)))
                 # GlucoseConcentration=fieldGlucose.get(pt)
-                
+
                 GlucoseConcentration=fieldGlucose[int(round(cell.xCOM)),int(round(cell.yCOM)),int(round(cell.zCOM))]
-                
+
                 # Proliferating Cells become Necrotic when GlucoseConcentration is low
                 if  GlucoseConcentration < 0.001 and mcs>1000:
                     cell.type = self.NECROTIC
@@ -178,20 +187,20 @@ class VolumeParamSteppable(SteppableBasePy):
                 #sNecrotic Cells shrink at a constant rate
                 numberOfNecroticCells = numberOfNecroticCells + 1
                 cell.targetVolume-=0.1
-        
+
         self.numberOfTumorCellList.append(numberOfTumorCells)
         self.numberOfVascularCellList.append(numberOfVascularCells)
         self.numberOfNeovascularCellList.append(numberOfNeovascularCells)
         self.numberOfNecroticCellList.append(numberOfNecroticCells)
-        
+
         self.pW.addDataPoint("Tumor Cells", mcs,numberOfTumorCells)
         self.pW.addDataPoint("Vascular Cells", mcs,numberOfVascularCells)
         self.pW.addDataPoint("Neovascular Cells",mcs,numberOfNeovascularCells)
         self.pW.addDataPoint("Necrotic Cells",mcs,numberOfNecroticCells)
- 
-    
+
+
     def finish(self):
-        print 'the number of tumor  cells',self.numberOfTumorCellList
+        print('the number of tumor  cells',self.numberOfTumorCellList)
         for index,i in enumerate( self.numberOfTumorCellList):
             vascularNum = self.numberOfVascularCellList[index]
             neovascularNum = self.numberOfNeovascularCellList[index]
@@ -199,28 +208,28 @@ class VolumeParamSteppable(SteppableBasePy):
             self.cellNumberFileOut.write('{0:d},{1:d},{2:d},{3:d},{4:d}\n'.format(index,i,vascularNum,neovascularNum,necroticNum)) # save the tumor cell number for each MCS
         self.cellNumberFileOut.close()
 
-        
+
 class MitosisSteppable(MitosisSteppableBase):
-    def __init__(self,_simulator,_frequency=1):
-        MitosisSteppableBase.__init__(self,_simulator, _frequency)
-     
+    def __init__(self, frequency=1):
+        MitosisSteppableBase.__init__(self, frequency)
+
     def step(self,mcs):
-        
+
         cells_to_divide=[]
-          
+
         for cell in self.cellList:
             if cell.type == self.PROLIFERATING and cell.volume>64:
                 cells_to_divide.append(cell)
             if cell.type== self.NEOVASCULAR and cell.volume>128:
                 cells_to_divide.append(cell)
 
-                     
+
         for cell in cells_to_divide:
 
             self.divideCellRandomOrientation(cell)
-            
-    def updateAttributes(self):    
-        self.parentCell.targetVolume /= 2.0 # reducing parent target volume                 
-        self.cloneParent2Child()            
-              
-                
\ No newline at end of file
+
+    def updateAttributes(self):
+        self.parentCell.targetVolume /= 2.0 # reducing parent target volume
+        self.cloneParent2Child()
+
+
